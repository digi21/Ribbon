<Window
    x:Class="RibbonGallery.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Digi21.WinUI.Ribbon" />
        <TextBlock
            Grid.Row="1"
            Style="{StaticResource BodyTextBlockStyle}"
            Text="The gallery is empty until the control exists. It will show the size variants, the groups, the hosted controls, the launcher flyout, light and dark, and a window you can narrow to watch the layout degrade."
            TextWrapping="Wrap" />
    </Grid>

</Window>
